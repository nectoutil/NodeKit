name: Release

on:
  workflow_run:
    workflows: ["Test"]
    types: [completed]
    branches: [main]

concurrency: ${{ github.workflow }}-${{ github.ref }}

env:
  CI: true
  LEFTHOOK: 0

jobs:
  build:
    name: Build
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/build.yml

  release:
    name: Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      pull-requests: write

    steps:
      - name: Generate GitHub App token
        id: generate_token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.NECTO_BOT_ID }}
          private-key: ${{ secrets.NECTO_BOT_KEY }}

      - name: Checkout Repo
        uses: actions/checkout@v6
        with:
          token: ${{ steps.generate_token.outputs.token }}

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: Install PNPM
        uses: pnpm/action-setup@v6

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Download build artifact
        uses: actions/download-artifact@v8
        with:
          name: ${{ needs.build.outputs.artifact-name }}
          path: packages

      - name: Strip devDependencies from packages
        uses: actions/github-script@v9
        with:
          github-token: ${{ steps.generate_token.outputs.token }}
          script: await import(`${process.env.GITHUB_WORKSPACE}/scripts/clean-publish.mjs`)

      - name: Create Release PR
        id: create_release_pr
        uses: changesets/action@v1
        with:
          title: 'Automated Package Release'
          createGithubReleases: true
          commit: 'Package Versioning'
          publish: pnpm exec changeset publish
        env:
          GITHUB_TOKEN: ${{ steps.generate_token.outputs.token }}
          NPM_TOKEN: ''
          NPM_CONFIG_PROVENANCE: true
          NPM_CONFIG_ACCESS: public
          # The "Strip devDependencies" step above mutates package.json files
          # in place, which makes pnpm's pre-exec deps-status check (added in
          # v10) see the lockfile as stale and abort `pnpm exec` before the
          # publish runs. The mutation is intentional and runner-local — it
          # never lands in the repo — so disable the check for this step only.
          npm_config_verify_deps_before_run: 'false'

      - name: Enable auto-merge on release PR
        if: steps.create_release_pr.outputs.pullRequestNumber
        uses: peter-evans/enable-pull-request-automerge@v3
        with:
          token: ${{ steps.generate_token.outputs.token }}
          pull-request-number: ${{ steps.create_release_pr.outputs.pullRequestNumber }}
          merge-method: squash
