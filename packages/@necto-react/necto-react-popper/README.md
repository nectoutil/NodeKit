# @necto-react/popper

React hooks, contexts, and components for positioning and floating elements.

Built on top of `@necto/popper` positioning engine.

## Structure

- **hooks/** - React hooks for positioning (`usePopper`, `useFloating`, etc.)
- **contexts/** - Context providers for popover state management
- **components/** - High-level components for common use cases
