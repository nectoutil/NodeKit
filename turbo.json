{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "style-dictionary#build": {
      "dependsOn": ["^build"],
      "outputs": [".dist/**"]
    },
    "handlebars#build": {
      "dependsOn": ["^build"],
      "outputs": [".dist/**"]
    }
  }
}
