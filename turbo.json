{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["build:style-dictionary"],
      "outputs": []
    },
    "build:style-dictionary": {
      "outputs": ["packages/@necto-styledictionary/dist/**"]
    }
  }
}
